{% extends 'base.html.twig' %}

{% block title %}User
{% endblock %}

{% block body %}

	<h1>{{user.nom}}
		{{user.prenom}}</h1>
	<a href="{{ path('app_compte_utilisateur') }}">
		<button>Paramètre du compte employé</button>
	</a>
	<a href="{{ path('app_home') }}">Retour</a>
{% endblock %}
